name: DeepAlphaClub
options:
  bundleIdPrefix: club.deepalpha
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  DeepAlphaCore:
    path: Core

targets:
  DeepAlphaClub:
    type: application
    platform: iOS
    sources:
      - path: App
    dependencies:
      - package: DeepAlphaCore
    # Keychain 需要 entitlement：缺了 SecItemAdd 会返回 -34018，
    # 模拟器上必现（真机走描述文件时也依赖它）。
    entitlements:
      path: App/DeepAlphaClub.entitlements
      properties:
        keychain-access-groups:
          - $(AppIdentifierPrefix)club.deepalpha.ios
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: 交易台
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        ITSAppUsesNonExemptEncryption: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: club.deepalpha.ios
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_STYLE: Automatic

schemes:
  DeepAlphaClub:
    build:
      targets:
        DeepAlphaClub: all
    run:
      config: Debug
    archive:
      config: Release
