# 上传对照

**展示截图**（ASC → 版本页 → 截屏），按此顺序传这 6 张 PNG：

1. `01_analysis_us.png`
2. `06_fullscreen.png`
3. `02_analysis_hk.png`
4. `03_learn.png`
5. `04_lesson_detail.png`
6. `05_query.png`

**不要**把下面这张一起传到版本页：

- `paywall_subscription_review.jpg` —— 这是**订阅项**的审核截图，
  传在 ASC → App 内购买项目 → 订阅项 → 审核信息 → 截屏。
  和展示截图是两个地方、两种用途。

源图在 `../screenshots-6.9/`，改文案或换标的后用
`../make_captioned.py` 重新生成即可（会同时输出 6.9 与 6.5 两套）。
