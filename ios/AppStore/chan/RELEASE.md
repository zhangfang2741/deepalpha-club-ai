# 缠论 App 发新版本清单

首次上架的完整流程在 `SUBMIT.md`（含 ASC 建 App、配内购、隐私问卷等一次性工作）。
**本文件是 1.0 上架之后，每发一个新版本要走的流程**，比首次短很多。

当前：**1.0 已上架**（App Store ID `6806500280`），正在准备 **1.1**。

---

## 本版（1.1）内容

1.0 提交审核（2026-08-31）之后的全部用户可见改动。本文件此前只记了分享/
手势/登录那一批，**「信号」Tab 及其后的一大批改动当时漏记了**——下表已补全，
下面几节（审核演示步骤、截图、审核备注）也已按新范围重新核对，提交前不用
再自己去翻 diff 找漏项：

| 变化 | 说明 |
|------|------|
| **新增「信号」Tab（雷达气泡图）** | 首页 Tab（登录后默认看到），按日重建所选指数成分股当天前 10 名买卖点信号，气泡大小=一二三类、颜色=方向、深浅=强弱、位置=新鲜度，可翻看历史交易日、切换美股/A股/港股与科技窄基/大盘宽基；页面下方已加免责声明 |
| **新增次级别确认（区间套）** | 日线/周线结果图下方一行结论，点开弹出低一级周期（30分钟/日线）确认图 |
| **新增「自选」Tab** | 登录用户可在结果页加星，「信号」页可单独扫自选池 |
| 结构讲解改版 | 图例术语、买卖点标签、新的「中枢阶段」示意图术语均可点击内联弹出学习页词条 |
| 分析结果长图分享 | 详情页分享按钮输出整页长图，带品牌脚与下载二维码 |
| 截屏即分享 | App 内截屏自动弹预览，可存相册或走系统分享面板 |
| 图表惯性与回弹 | 平移甩动后继续滑行，滑到边缘橡皮筋回弹 |
| MACD 支持平移缩放 | 副图接上与主图相同的手势，光标竖线联动 |
| 光标可拖动 | 点选 K 线后滑动即可移动十字光标 |
| 手机号注册登录 | 中国大陆 +86 |
| 英文界面补全 | 补齐缺失译文；地区未命中时改按系统语言判断 |
| 中英文案缺口修复 | 「信号」页与「中枢阶段」相关 9 条文案此前只有英文译文、zh-Hans 表里缺失（中文系统下会显示成英文），已补全并核对两个语言表 key 完全对齐 |

文案见 `store-listing.md` 第五节「1.1」，中英各一份——**「信号」Tab 是这版
最大的可见变化，第五节的 1.1 文案目前也还没提它，发版前请一并更新，
不然商品页描述和应用实际内容对不上，有 2.3.1 风险**。

---

## 代码侧已完成

- [x] 版本号递增：`MARKETING_VERSION = 1.1`、`CURRENT_PROJECT_VERSION = 2`
- [x] 相册写入权限文案 `NSPhotoLibraryAddUsageDescription`（Info.plist 英文 + `zh-Hans.lproj` 中文）
- [x] 更新说明文案（中英）
- [x] `PrivacyInfo.xcprivacy` 无需改动 —— 相册是**写入**（`.addOnly`），不收集数据，
      隐私营养标签也不用动；新增的语言偏好仍走 UserDefaults，已在 CA92.1 覆盖内
- [x] 「信号」气泡图底部补免责声明（此前没有，App Store 提交前发现并补上）
- [x] 气泡大小口径两次调整：拥挤缩放上限 0.5→0.32（整体偏大）、一二三类直径
      70/88/108→78/90/102（三档差异过大）
- [x] zh-Hans.lproj 补齐 9 条此前只有英文译文、中文表缺失的 key（信号页 3 条 + 中枢阶段
      确认 5 条 + 「历史」1 条），两个语言表的 key 集合已核对完全一致；顺带清掉一组
      重复的「分析」key（两个文件里都是无害重复，一并删掉）
- [x] `review-notes.md` / `store-listing.md` 第五节 已按「信号」Tab 等新功能重新核对更新
      （见下方「本版（1.1）内容」）——**Xcode 归档、真机测试、ASC 表单与截图上传仍需你本人
      完成，这个环境没有 macOS/Xcode 无法代做**

---

## 🔴 提交前必做（容易漏的两项）

### 1. 重建审核演示账号

`SUBMIT.md` 末尾要求上架后删除 `appreview@deepalpha.club`（弱密码账号，留着是风险）。
**如果当时删了，这次必须重建**，否则审核员登不进去，必被 2.1 打回。

```
appreview@deepalpha.club
AppReview2026
```

建完自己登一次，确认能看到分析结果页。

### 2. 真机验证（模拟器测不了或测不准的）

本版改动集中在手势、离屏渲染和本地化，这三类都得在真机上看：

- [ ] **保存到相册**：首次点会弹系统授权框（新权限，1.0 没有），确认文案语言正确、允许后相册里确实有图
- [ ] **分享长图**：图层开关那一排在图里（此前会整排消失）、截图与品牌脚**左右边缘对齐**、底部免责条完整不被裁
- [ ] **MACD 手势**：详情页和全屏页，手指落在 MACD 上能左右拖、能双指缩放，主副图不错开
- [ ] **图标名与语言**：本版把开发区域从 `zh_CN` 改成了 `en`。中文系统下确认桌面图标仍是「缠论」；
      把系统语言换成英文（或地区换成非中国大陆）确认界面为英文、图标名为 DeepAlpha
- [ ] **全屏转屏**：进全屏转横屏 → 退出后回到竖屏（回归项，失败会卡横屏）
- [ ] **订阅**：沙盒账号购买 + 恢复购买（回归项）
- [ ] **「信号」气泡图**：三个市场都点一遍切换、拖一颗气泡看回弹、点气泡能正确跳到该股详情页；
      底部免责声明常驻可见（本次改动新加，之前没有）；把系统语言切到英文，确认气泡页文案
      不会露出中文（本次一并修了 9 条 zh-Hans 缺失译文，实机应确认不再出现英文夹杂）
- [ ] **次级别确认**：日线结果页图表下方点开次级别，能看到 30 分钟确认图；周线同理配日线
- [ ] **自选**：结果页加星 → 「信号」页切到自选池能看到该股

---

## 提交流程

1. **合并到 master**，确认 master 上 `xcodebuild` 通过后再打包，避免发出去的包不在主干上
2. **Xcode → Product → Archive**
   - scheme 选 DeepAlphaChan，设备选 Any iOS Device
   - ⚠️ Archive 前把 Edit Scheme → Run → Options 的 **StoreKit Configuration 置为 None**，
     否则本地测试配置可能影响验证（发布包本身已排除该文件）
3. **Distribute App → App Store Connect → Upload**，等 5–15 分钟处理完
4. **ASC → App → 左侧「+ 版本」→ 填 1.1**
   - 「此版本新增内容」：粘贴 `store-listing.md` 第五节 1.1 的中英文案，**两个语言各填一次**
   - 构建版本：选 1.1 (2)
   - 截图：现有 6 张仍然有效（核心界面未变），不换也能过；想展示分享功能可另加
   - 描述、关键词、推广文本：**不用动**（1.0 已通过，动了要重审）
5. **审核信息**：演示账号照旧填；备注**整段重贴** `review-notes.md`
   第二节「可直接粘贴的备注正文」（中英两版都在里面），**不要沿用 1.0 那份**。

   > ⚠️ 为什么必须重贴：1.0 那份备注第六节写的是「App 不采集任何位置、通讯录、
   > **相册**或健康数据」/「collects no location, contacts, **photos** or health
   > data」。本版新增了相册写入，这句话就成了不实陈述——比漏写更容易被判
   > 2.3.1。已改成「不读取相册；相册权限仅用于写入用户主动生成的分析图
   > （add-only）」，并新增第七节逐条说明本版新功能与新权限的复现路径。

6. 发布方式建议仍选 **手动发布**，通过后自己验一遍再放出
7. 点「提交以供审核」

> 更新版本的审核通常比首审快，24 小时内居多。

---

## 如果被拒

拒因话术见 `review-notes.md` 末尾。本版新增了分享功能，多一个可能的拒因：

**5.2.5（转发内容）**：分享图里带 App 品牌和下载二维码，属于自有内容推广，不涉及第三方
商标或版权素材。图中所有数据均为本 App 算法生成，且图上常驻免责声明
（「算法自动生成，仅供技术研究，不构成投资建议」）。
