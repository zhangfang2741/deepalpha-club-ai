import SwiftUI

// 临时占位：Task 16/17 替换为真组件后删除本文件。
struct TradingDeskView: View {
    var body: some View { Text("交易台（Task 16）") }
}

struct HistoryListView: View {
    var body: some View { Text("历史（Task 17）") }
}
